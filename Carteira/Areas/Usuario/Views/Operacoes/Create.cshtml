@model Carteira.Entity.Operacao

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_DashboardUser.cshtml";
}

<h1>Create</h1>

<h4>Configuracao</h4>
<hr />

@using (Html.BeginForm("Create", "Operacoes", FormMethod.Post))
{
    <div class="form-group">
        @*@Html.DisplayNameFor(model => model.Ativo.Nome)
        @Html.EditorFor(model => model.Ativo)
        @Html.ValidationMessageFor(model => model.Ativo)*@
    </div>

    <div class="form-group">
        <input type="submit" value="Create" class="btn btn-primary" />
    </div>
}
<div>
    @Html.ActionLink("Voltar", "Index", "Operacoes", new { Area = "Usuario" })
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
